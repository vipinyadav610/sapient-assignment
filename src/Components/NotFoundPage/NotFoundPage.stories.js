import React from 'react';
import { storiesOf } from '@storybook/react';
import NotFoundPage from './NotFoundPage';
storiesOf('NotFoundPage', module).add('NotFoundPage', () => (
  <div>
    <NotFoundPage />
  </div>
));
